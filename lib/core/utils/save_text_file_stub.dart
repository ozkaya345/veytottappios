Future<String?> saveTextFile({
  required String suggestedName,
  required String content,
  String mimeType = 'text/plain',
}) {
  throw UnsupportedError('saveTextFile is not supported on this platform');
}
